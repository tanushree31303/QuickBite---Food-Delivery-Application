# Copy this file to `.env` and replace with your own Razorpay Key ID.
# NEVER commit the real `.env` file. The Key ID is not a secret, but the
# Key Secret must only live on the server (backend environment variables).
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxxxxx